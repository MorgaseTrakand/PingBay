import React from "react";

type Props = {
  // define props here if needed, e.g. title?: string;
};

const HomePage: React.FC<Props> = () => {
  return (
    <div>
      <h1 className="text-1xl">HI</h1>
    </div>
  );
};

export default HomePage;
